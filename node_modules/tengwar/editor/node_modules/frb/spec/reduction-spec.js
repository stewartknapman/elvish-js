/*
var bind = require("../bind");
describe("reduction", function () {
    it("<-", function () {
        var foo = {a: {b: 10}};
        var bar = {a: {b: 10}};
        var cancel = bind(foo, "a.b", {
            "<->": "a.b",
            source: bar,
            trace: true
        });
        console.log("---");
        foo.a = {b: 40}; // orphan a and replace
        expect(bar.a.b).toBe(40); // updated
    });
    it("->", function () {
        console.log("--------");
        var foo = {a: {b: 10}};
        var bar = {a: {b: 10}};
        var cancel = bind(bar, "a.b", {
            "<->": "a.b",
            source: foo,
            trace: true
        });
        console.log("---");
        foo.a = {b: 40}; // orphan a and replace
        expect(bar.a.b).toBe(40); // updated
    });
});
*/
