
if (typeof jQuery !== "undefined") {
    jQuery.fn.tengwar = function () {
        this.each(function () {
            var that = jQuery(this);
            that
            .html(transcribe(that.html()))
            .addClass("tengwar");
        });
    };
}

